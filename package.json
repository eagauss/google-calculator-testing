{
  "name": "google-calculator-testing",
  "version": "1.0.0",
  "description": "Automated end-to-end test suite for the Google Calculator component.",
  "author": "Emil Akhadov <https://www.linkedin.com/in/emil-akhadov/>",
  "scripts": {
    "test": "cypress run --headed --browser chrome",
    "cy:run": "cypress run --headed --browser chrome",
    "cy:open": "cypress open",
    "cy:verify": "cypress verify",
    "audit": "npm audit --production --audit-level=critical"
  },
  "devDependencies": {
    "cypress": "^14.4.0",
    "@cypress/xpath": "^2.0.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
